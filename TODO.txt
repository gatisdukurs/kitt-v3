NOW: fix HTMX via Layout htmx support, 
also maybe the sending should happen by returning response instead of writing to it from handlers?
this would give option to switch between regular or htmx, right?

NEXT: Refactor module concept
MEXT: Try Model concept

Get Rid of KittContext and use just render context, like Controller.ViewCtx()

NEXT: Think about how to propagte errors from handlers - with return Send()
NEXT: Queue Workers, like pulling data from external apis and stuff?
NEXT: Nicer API for controllers, working with db, handling errors, etc.
NEXT: Maybe create template helpers for form builders and stuff, for css, js, assets, func registration.
NEXT: Figure out template naming for htmx ones vs full.
NEXT: Create frontend for displaying added pages.
NEXT: Form builder?
NEXT: Think about reusable layouts?

func (c IndexController) GetIndex(rctx router.RouteCtx) router.RouteResponse {
	// View
	view := c.Layout("admin.layout")
	content := c.Partial("admin.content")
	navigation := c.Navigation(rctx)

	view.WithPartial("content", content)
	view.WithPartial("navigation", navigation)
	// Send
	return c.Response(view).WithStatus(200)
}