== TODO

NOW: 

2. Add edit for pages.
3. change toaster to an OOB object capability, so I can return same list of pages, but with error msg.
and then I can use that also for success messages, like in forms, etc.

NEXT: Make kitt config, like db etc. And maybe repo init thing that already boots repos from settings?
NEXT: Services concept.

NEXT: Figure out how to solve this issue with changing attributes and sharing action:

f.WithAction(fmt.Sprintf("/admin/pages/%d/edit", id))
f.WithHTMXPost()

NEXT: Refactor module concept.

NEXT: Add loading for HTMX?

Get Rid of KittContext and use just render context, like Controller.ViewCtx()

NEXT: Queue Workers, like pulling data from external apis and stuff?
NEXT: Create frontend for displaying added pages.
NEXT: Write some tests for controllers and see how it goes.

Form/Templates:

Think about url herlper functions for templates - NAMED ROUTES that I can then use in templates
Refactor attributes for form, that they are all rendered in tags as {{ .Attributes }}
Also add attributes that can't be set like "value", "id", etc
Also refactor maybe out attribute and all html-ish things so I dont have duplicate attr rendering and stuff from forms.
Make form tests less depending on template changes of elements

DB/Repos:

==== SelectBuilder

Done for now

Distinct
Join
LeftJoin
GroupBy
Having

==== InsertBuilder

DONE for now, the rest is optional

ValuesMap
DefaultValues
FromSelect
OnConflict
DoNothing
DoUpdate...
Returning

==== UpdateBuilder

DONE for now, the rest is optional for now

SetExpr
SetSubquery
OrderBy
Limit
Offset
Returning

==== DeleteBuilder

Done for now

OrderBy
Limit
Offset
Returning